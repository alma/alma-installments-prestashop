watch:
  directories:
    - src
    - tests
  exclude:
    - vendor
    - '**/index.php'
  fileMask: '*.php'
  ignoreDotFiles: true
  ignoreVCS: true
  ignoreVCSIgnored: false
phpunit:
  binaryPath: vendor/bin/phpunit
  arguments: '-c phpunit.ci.xml'
