<?xml version="1.0"?>
<ruleset xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" name="Alma Woocommerce Gateway"
         xsi:noNamespaceSchemaLocation="https://raw.githubusercontent.com/squizlabs/PHP_CodeSniffer/master/phpcs.xsd">

    <description>A custom set of rules to check for Alma Prestashop Plugin</description>

    <!-- Exclude the Composer Vendor directory. -->
    <exclude-pattern>*/vendor/*</exclude-pattern>
    <exclude-pattern>*/translations/*</exclude-pattern>

    <!-- Inclure les standards PSR-12 et Symfony -->
    <rule ref="PSR12" />
    <rule ref="Symfony" />

    <!-- Check for PHP cross-version compatibility. -->
    <!--
    To enable this, the PHPCompatibilityWP standard needs
    to be installed.
    See the readme for installation instructions:
    https://github.com/PHPCompatibility/PHPCompatibilityWP
    For more information, also see:
    https://github.com/PHPCompatibility/PHPCompatibility
    -->
    <config name="testVersion" value="5.6-"/>

    <config name="ignore_warnings_on_exit" value="1"/>

</ruleset>
